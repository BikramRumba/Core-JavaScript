/* let course = 'JavaScript is cool';
console.log(course.length);

// trim() methods remove extra white spaces

console.log(course.toUpperCase());
console.log(course.toLowerCase());

// indexOf() methods is used to find the index number
console.log(course.indexOf('a')); // 1 in this case
console.log(course.lastIndexOf('a')); //last occurence of the current letter or word which is 3 in this case;

//slice() methods. Slice extracts the specified index.

console.log(course.slice(0, 10)); //0 is first index and 10 is the last index.

// replace() method we pass two parameters 1st one is the one being replaced, 2nd one is the one replacing
console.log(course.replace('cool', 'great'));

// chartAt() method. it is the opposite of indexOf()method;
// indexOf() finds the index number whereas charAt() returns the value of that index  number
console.log(course.charAt(4));
 */

// TASKS

let x = 'I am watching Dev Dreamer.';
console.log(x.toUpperCase());

console.log(x.slice(14, 25));
